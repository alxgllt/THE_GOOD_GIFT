    <div class="error-messages">
    </div>
    <div class="form-inline">
      <%= form_tag(select_tags_path, :method => "get", id: "select-genre") do %>
      <%= text_field_tag :search_name, params[:search_name], placeholder: "Prénom", class: "form-control"%>
      <!-- <span class="input-symbol-euro"> -->
      <%= text_field_tag :search_price, params[:search_price], placeholder: "Budget", class: "form-control"%>
      <!-- </span> -->
      <%= select_tag :search_gender, options_for_select(["Homme", "Femme"], params[:search_gender]), prompt: "Genre", class: "form-control", id: "sexe" %>
      <%= text_field_tag :search_date, params[:search_date], placeholder: "Date de la remise du cadeau", class: "form-control hidden", id: "datepicker"%>
      <%= submit_tag "Je lui fais plaisir", class: "btn btn-primary" %>
      <% end %>
    </div>


<% content_for :after_js do %>
<script>
 $("#select-genre").on("submit", function() {
  var paragraph = $('<p>').text("Votre ami(e) est il un homme ou une femme ?");
  if ($( "#sexe option:selected" ).text() !== "Genre") {
    return true;
  } else {
    $("#sexe").toggleClass('red-message-error');
    $('.error-messages').html(paragraph);
    return false; // prevent it from being submitted (errors, etc.)
  }
});
</script>
<% end %>
