
<% #bundle page flow view : n°3 %>

<h1> Bundle pages </h1>

<%= render 'products/search_bar_products' %>

<div class="wrapper-bundle">
  <div class="container">

    <p> big : <%= @matching_list[:big].first&.name %> </p>
    <p> medium : <%= @matching_list[:medium].first&.name %></p>
    <p> small <%= @matching_list[:small].first&.name %></p>

   <%= form_tag orders_path do %>
    <%= hidden_field_tag 'product_id', @matching_list[:big].first&.id %>
    <%= hidden_field_tag 'product_id', @matching_list[:medium].first&.id %>
    <%= hidden_field_tag 'product_id', @matching_list[:small].first&.id %>
    <%= submit_tag 'Passer commande', class: 'btn btn-primary' %>
   <% end %>

  </div>
</div>



    <div class="wrapper-bundle">
      <div class="container">
        <div class="row">
          <div class="bundle">
            <% @products.each do |product| %>
            <div class="col-xs-6 col-md-4 col-sm-4 ">
              <div class="product">
                <ul><%= product.name %></ul>
                <ul><%= product.brand %></ul>
                <ul>genre: <%= product.gender %></ul>
                <ul><%= cl_image_tag product.image, height: 70, crop: :fill %></ul>
              </div>
            </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
