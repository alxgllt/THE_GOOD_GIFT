console.log("test")

// 1. refaire le matching et rafficher le carousel
$(".matching-list-container").html("<%= j render 'carts/matching_list', matching_list: @matching_list, cart: @cart %>");

// 2. mettre a jour le nouveau produit
var new_cart_product = $("<%= j render 'carts/cart_product_display', cart:@cart, cart_product: @cart_product %>").hide();
$('.items').append(new_cart_product);
new_cart_product.slideDown();

// modal du cart
$('.cart-content').prepend("<%= j render 'carts/modal_display', cart: @cart %>");

// JS UPDATES
$('.gift-carousel').slick(); // reactivate carousel
$('.progress-wrap').data('total', '<%= @cart.total %>'); // update progress bar total
